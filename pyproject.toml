[tool.ruff]
line-length = 120
target-version = "py312"
extend-exclude = [
    "binaries",
    "LiveFT2.py",
    "LiveFT_PyQt.py",
]

[tool.ruff.format]
quote-style = "double"
indent-style = "space"

[tool.ruff.lint]
select = ["E", "F", "W", "I"]
