[tool.ruff]
line-length = 120
target-version = "py312"
extend-exclude = [
    "binaries",
    "experiments",
]

[tool.ruff.format]
quote-style = "double"
indent-style = "space"

[tool.ruff.lint]
select = ["E", "F", "W", "I"]
