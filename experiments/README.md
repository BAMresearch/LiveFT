# Experiments

These scripts are prototype UI experiments and are not part of the supported
`LiveFT.py` application or the default test, lint, or release surface.

- `LiveFT2.py`: PyQt FFT prototype
- `LiveFT_PyQt.py`: PyQt camera-view prototype
